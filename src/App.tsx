import type { ReactElement } from "react";
import { Stack } from "@mui/material";

function App(): ReactElement {
  return (
    <Stack>
      <h1>MUI Form</h1>
    </Stack>
  );
}

export default App;
